CREATE TABLE company
(
  name  VARCHAR2(30 CHAR)                        NOT NULL,
  owner  INT(9)                                 NULL
);
CREATE TABLE film
  (
    title  VARCHAR2(30 CHAR)                        NOT NULL
  );

COMMIT;
